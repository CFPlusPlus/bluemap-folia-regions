name: $name
version: $version
main: $main
api-version: "$apiVersion"
website: https://github.com/CFPlusPlus/bluemap-folia-regions
authors:
  - CFPlusPlus
folia-supported: true
dependencies:
  server:
    BlueMap:
      load: AFTER
      required: true
